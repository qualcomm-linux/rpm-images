# Firmware Loader Compression Support
#
# Enable compressed firmware loading to reduce firmware file sizes
# and improve boot performance. The kernel will automatically
# decompress firmware files on-the-fly.
CONFIG_FW_LOADER_COMPRESS=y

CONFIG_FW_LOADER_COMPRESS_ZSTD=y
